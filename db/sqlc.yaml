version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "database"
        out: "../internal/foundation/database"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
