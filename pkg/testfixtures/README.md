# Synthetic test fixtures

Reusable fixtures must be synthetic, deterministic, explicitly marked as simulated, and free of real customer or provider data. The Phase 0 demo identity currently lives beside its local provider contract under `internal/identity/provider`.
